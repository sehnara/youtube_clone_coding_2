import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import App from './app';

ReactDOM.render(
  <React.StrictMode> // 'use strict'와 같은 역할
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);

